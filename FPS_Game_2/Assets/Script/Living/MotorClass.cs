using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// MotorClass contains the method for moving and physics implmentation for both
/// the player and the NPC.
/// </summary>
public class MotorClass : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
